---
description: Per-project notification sounds — pack, pause/resume, set, add, etc.
argument-hint: [subcommand] [args...]
allowed-tools: Bash(bash:*)
---

!`bash "${CLAUDE_PLUGIN_ROOT}/hooks-handlers/notify-sound.sh" $ARGUMENTS`

Print the script output above verbatim and stop. No preface, no summary, no commentary.
